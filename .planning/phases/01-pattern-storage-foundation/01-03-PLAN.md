---
phase: 01-pattern-storage-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .knowledge/patterns/layout/*.yaml
  - .knowledge/patterns/forms/*.yaml
  - .knowledge/patterns/navigation/*.yaml
  - .knowledge/patterns/micro-interactions/*.yaml
  - .knowledge/patterns/data-display/*.yaml
  - .knowledge/patterns/feedback/*.yaml
  - .knowledge/patterns/authentication/*.yaml
autonomous: true

must_haves:
  truths:
    - "At least 10 seed patterns exist across categories"
    - "Every category has at least 1 pattern"
    - "Each pattern has valid YAML structure passing validation"
    - "Patterns include principles AND code examples"
  artifacts:
    - path: ".knowledge/patterns/layout/"
      provides: "Layout design patterns"
      contains: "*.yaml files"
    - path: ".knowledge/patterns/forms/"
      provides: "Form design patterns"
      contains: "*.yaml files"
    - path: ".knowledge/patterns/navigation/"
      provides: "Navigation design patterns"
      contains: "*.yaml files"
    - path: ".knowledge/patterns/micro-interactions/"
      provides: "Micro-interaction patterns"
      contains: "*.yaml files"
    - path: ".knowledge/patterns/data-display/"
      provides: "Data display patterns"
      contains: "*.yaml files"
  key_links:
    - from: ".knowledge/patterns/**/*.yaml"
      to: ".knowledge/schema/pattern.schema.ts"
      via: "validates against PatternSchema"
      pattern: "category.*tags.*frameworks.*principles"
---

<objective>
Create initial seed patterns to establish quality baseline and demonstrate system functionality.

Purpose: STOR-06 requires 10-20 seed patterns across 5+ categories. These patterns demonstrate the system works and provide quality examples for future extraction.
Output: 12-15 validated YAML pattern files covering all 7 categories.
</objective>

<execution_context>
@/Users/khairul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khairul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pattern-storage-foundation/01-RESEARCH.md
@.planning/phases/01-pattern-storage-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create layout and navigation patterns</name>
  <files>
    .knowledge/patterns/layout/css-grid-system.yaml
    .knowledge/patterns/layout/flexbox-centering.yaml
    .knowledge/patterns/layout/responsive-card-grid.yaml
    .knowledge/patterns/navigation/sticky-navbar.yaml
    .knowledge/patterns/navigation/mobile-drawer.yaml
  </files>
  <action>
    Create 5 high-quality patterns:

    1. CSS Grid System (layout)
       - Principles: 12-column grid, minmax for flexibility, gap for consistent spacing
       - Code: React with Tailwind, vanilla CSS
       - Tags: grid, responsive, layout, columns
       - Accessibility: landmark regions, logical reading order

    2. Flexbox Centering (layout)
       - Principles: Perfect centering techniques, when to use which approach
       - Code: Multiple centering patterns (items-center, m-auto, etc.)
       - Tags: flexbox, centering, alignment
       - Accessibility: none needed for pure layout

    3. Responsive Card Grid (layout)
       - Principles: Auto-fit/auto-fill, aspect-ratio for images, hover states
       - Code: Product card grid example
       - Tags: cards, grid, responsive, e-commerce
       - Accessibility: alt text for images, keyboard focus

    4. Sticky Navbar (navigation)
       - Principles: Sticky positioning, scroll shadow, z-index management
       - Code: Header that sticks on scroll with shadow reveal
       - Tags: navbar, sticky, header, scroll
       - Accessibility: skip link, landmark nav

    5. Mobile Drawer (navigation)
       - Principles: Off-canvas pattern, touch gestures, focus trap
       - Code: Slide-out drawer with backdrop
       - Tags: drawer, mobile, menu, sidebar
       - Accessibility: focus trap, escape to close, aria-expanded

    Use savePattern() from the lib to write each pattern, ensuring validation.
  </action>
  <verify>
    - ls .knowledge/patterns/layout/*.yaml (3 files)
    - ls .knowledge/patterns/navigation/*.yaml (2 files)
    - Each file passes PatternSchema validation
  </verify>
  <done>5 layout and navigation patterns created with full metadata, principles, and code examples</done>
</task>

<task type="auto">
  <name>Task 2: Create forms and feedback patterns</name>
  <files>
    .knowledge/patterns/forms/floating-label-input.yaml
    .knowledge/patterns/forms/form-validation-states.yaml
    .knowledge/patterns/feedback/toast-notification.yaml
    .knowledge/patterns/feedback/loading-skeleton.yaml
  </files>
  <action>
    Create 4 patterns:

    1. Floating Label Input (forms)
       - Principles: Label animation, focus states, placeholder behavior
       - Code: Input with animated floating label on focus/content
       - Tags: input, label, animation, focus
       - Accessibility: always visible label, proper association

    2. Form Validation States (forms)
       - Principles: Error styling, success indicators, inline messages
       - Code: Input with error/success states, helper text
       - Tags: validation, error, form, states
       - Accessibility: aria-invalid, aria-describedby, live regions

    3. Toast Notification (feedback)
       - Principles: Non-blocking feedback, auto-dismiss, positioning
       - Code: Toast component with variants (success, error, info)
       - Tags: toast, notification, alert, feedback
       - Accessibility: role="status", aria-live="polite"

    4. Loading Skeleton (feedback)
       - Principles: Content placeholder, pulse animation, layout preservation
       - Code: Skeleton screens for cards, text, avatars
       - Tags: skeleton, loading, placeholder, animation
       - Accessibility: aria-busy, aria-label for loading state

    Use savePattern() to write each with validation.
  </action>
  <verify>
    - ls .knowledge/patterns/forms/*.yaml (2 files)
    - ls .knowledge/patterns/feedback/*.yaml (2 files)
    - Each file passes PatternSchema validation
  </verify>
  <done>4 forms and feedback patterns created with full metadata and accessibility notes</done>
</task>

<task type="auto">
  <name>Task 3: Create micro-interactions, data-display, and authentication patterns</name>
  <files>
    .knowledge/patterns/micro-interactions/button-hover-effect.yaml
    .knowledge/patterns/micro-interactions/ripple-effect.yaml
    .knowledge/patterns/data-display/data-table.yaml
    .knowledge/patterns/data-display/stat-card.yaml
    .knowledge/patterns/authentication/login-form.yaml
  </files>
  <action>
    Create 5 patterns:

    1. Button Hover Effect (micro-interactions)
       - Principles: Subtle scale, color shift, shadow lift, timing curves
       - Code: Button with hover animation (transform, shadow, transition)
       - Tags: button, hover, animation, interaction
       - Accessibility: no reliance on hover for essential info

    2. Ripple Effect (micro-interactions)
       - Principles: Click origin tracking, circular expansion, fade out
       - Code: Material-style ripple on button click
       - Tags: ripple, click, material, feedback
       - Accessibility: visual only, action still works without

    3. Data Table (data-display)
       - Principles: Column alignment, row hover, sticky header, responsive
       - Code: Table with sorting indicators, zebra striping
       - Tags: table, data, grid, sorting
       - Accessibility: proper th/td, scope attributes, caption

    4. Stat Card (data-display)
       - Principles: Number emphasis, trend indicator, compact layout
       - Code: Dashboard stat card with icon, value, label, change%
       - Tags: stats, dashboard, card, metrics
       - Accessibility: proper heading hierarchy, sr-only labels

    5. Login Form (authentication)
       - Principles: Clear labels, password toggle, error handling, submit state
       - Code: Login form with email/password, remember me, forgot link
       - Tags: login, auth, form, password
       - Accessibility: autocomplete attributes, label association, error announcement

    Use savePattern() to write each with validation.
  </action>
  <verify>
    - ls .knowledge/patterns/micro-interactions/*.yaml (2 files)
    - ls .knowledge/patterns/data-display/*.yaml (2 files)
    - ls .knowledge/patterns/authentication/*.yaml (1 file)
    - All files pass PatternSchema validation
    - Total pattern count: 14 patterns
  </verify>
  <done>5 remaining patterns created, achieving 14 total seed patterns across all 7 categories</done>
</task>

</tasks>

<verification>
1. Count total patterns:
   ```bash
   find .knowledge/patterns -name "*.yaml" | wc -l
   # Should be 14
   ```

2. Verify all categories have patterns:
   ```bash
   for dir in .knowledge/patterns/*/; do
     echo "$dir: $(ls "$dir"*.yaml 2>/dev/null | wc -l) patterns"
   done
   ```

3. Validate all patterns:
   ```typescript
   import { listPatterns, loadPattern } from '.knowledge/lib';

   const files = await listPatterns();
   for (const file of files) {
     await loadPattern(file); // Throws if invalid
   }
   console.log(`${files.length} patterns validated`);
   ```

4. Test search functionality:
   ```typescript
   import { searchByTags, filterByCategory } from '.knowledge/lib';

   const gridPatterns = await searchByTags(['grid']);
   console.log(`Patterns tagged 'grid': ${gridPatterns.length}`);

   const layoutPatterns = await filterByCategory('layout');
   console.log(`Layout patterns: ${layoutPatterns.length}`);
   ```
</verification>

<success_criteria>
- 14+ seed patterns exist in .knowledge/patterns/
- All 7 categories have at least 1 pattern
- Every pattern has: name, category, tags, frameworks, principles
- Every pattern passes PatternSchema validation
- Code examples provided for React and vanilla at minimum
- Accessibility notes included where relevant
- Search by tags returns correct results
- Filter by category returns correct results
</success_criteria>

<output>
After completion, create `.planning/phases/01-pattern-storage-foundation/01-03-SUMMARY.md`
</output>

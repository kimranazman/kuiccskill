---
phase: 04-multi-framework-docs
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .knowledge/patterns/layout/css-grid-system.yaml
  - .knowledge/patterns/layout/flexbox-centering.yaml
  - .knowledge/patterns/layout/responsive-card-grid.yaml
  - .knowledge/patterns/navigation/sticky-navbar.yaml
  - .knowledge/patterns/navigation/mobile-drawer-menu.yaml
autonomous: true

must_haves:
  truths:
    - "All layout patterns have documentation field with description and usage"
    - "All navigation patterns have documentation field with description and usage"
    - "All 5 patterns have framework_compatibility with version strings"
    - "All 5 patterns have Vue and Svelte code examples"
  artifacts:
    - path: ".knowledge/patterns/layout/css-grid-system.yaml"
      provides: "Documented grid pattern with all framework examples"
    - path: ".knowledge/patterns/navigation/sticky-navbar.yaml"
      provides: "Documented navbar pattern with all framework examples"
  key_links:
    - from: ".knowledge/patterns/layout/css-grid-system.yaml"
      to: ".knowledge/schema/pattern.schema.ts"
      via: "validation"
      pattern: "PatternSchema"
---

<objective>
Add documentation and multi-framework code examples to layout and navigation patterns.

Purpose: Update the 5 layout and navigation patterns with comprehensive documentation, framework compatibility metadata, and code examples for Vue, Svelte (in addition to existing React and vanilla).
Output: Fully documented patterns with code_examples for all 4 frameworks.
</objective>

<execution_context>
@/Users/khairul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khairul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-framework-docs/04-RESEARCH.md
@.knowledge/schema/pattern.schema.ts
@.knowledge/patterns/layout/css-grid-system.yaml
@.knowledge/patterns/navigation/sticky-navbar.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update layout patterns with documentation</name>
  <files>.knowledge/patterns/layout/css-grid-system.yaml, .knowledge/patterns/layout/flexbox-centering.yaml, .knowledge/patterns/layout/responsive-card-grid.yaml</files>
  <action>
For each layout pattern (css-grid-system, flexbox-centering, responsive-card-grid):

1. Add documentation field:
```yaml
documentation:
  description: "[One paragraph describing what this pattern does and when to use it]"
  usage: "[How to use this pattern in your project]"
  parameters:
    - name: cols
      type: number
      description: Number of grid columns
      required: false
      default: "12"
  best_practices:
    - "[Best practice 1]"
    - "[Best practice 2]"
```

2. Add framework_compatibility field:
```yaml
framework_compatibility:
  react: "18.0+"
  vue: "3.0+"
  svelte: "4.0+"
  vanilla: true
```

3. Add Vue code example to code_examples:
```yaml
code_examples:
  vue: |
    <script setup lang="ts">
    interface Props {
      cols?: number;
    }
    const props = withDefaults(defineProps<Props>(), { cols: 12 });
    </script>
    <template>
      <div :class="['grid', `grid-cols-${cols}`]">
        <slot />
      </div>
    </template>
```

4. Add Svelte code example to code_examples:
```yaml
code_examples:
  svelte: |
    <script lang="ts">
    export let cols: number = 12;
    </script>
    <div class="grid grid-cols-{cols}">
      <slot />
    </div>
```

Follow the existing patterns for React and vanilla as reference for component structure and API.
  </action>
  <verify>
Run pattern validation: `npx tsx .knowledge/schema/validate-patterns.ts`
All 3 layout patterns should validate.
  </verify>
  <done>All 3 layout patterns have documentation, framework_compatibility, and Vue/Svelte code examples</done>
</task>

<task type="auto">
  <name>Task 2: Update navigation patterns with documentation</name>
  <files>.knowledge/patterns/navigation/sticky-navbar.yaml, .knowledge/patterns/navigation/mobile-drawer-menu.yaml</files>
  <action>
For each navigation pattern (sticky-navbar, mobile-drawer-menu):

1. Add documentation field:
```yaml
documentation:
  description: "[One paragraph describing what this pattern does and when to use it]"
  usage: "[How to use this pattern in your project]"
  parameters:
    - name: items
      type: "NavItem[]"
      description: Array of navigation items
      required: true
    - name: isSticky
      type: boolean
      description: Whether navbar should stick to top on scroll
      required: false
      default: "true"
  best_practices:
    - "[Best practice 1]"
    - "[Best practice 2]"
```

2. Add framework_compatibility field:
```yaml
framework_compatibility:
  react: "18.0+"
  vue: "3.0+"
  svelte: "4.0+"
  vanilla: true
```

3. Add Vue code example:
```yaml
code_examples:
  vue: |
    <script setup lang="ts">
    interface NavItem { label: string; href: string; }
    interface Props {
      items: NavItem[];
      isSticky?: boolean;
    }
    const props = withDefaults(defineProps<Props>(), { isSticky: true });
    </script>
    <template>
      <nav :class="{ 'sticky top-0': isSticky }">
        <a v-for="item in items" :key="item.href" :href="item.href">
          {{ item.label }}
        </a>
      </nav>
    </template>
```

4. Add Svelte code example to code_examples.
  </action>
  <verify>
Run pattern validation: `npx tsx .knowledge/schema/validate-patterns.ts`
All 2 navigation patterns should validate.
  </verify>
  <done>All 2 navigation patterns have documentation, framework_compatibility, and Vue/Svelte code examples</done>
</task>

</tasks>

<verification>
- [ ] All 5 patterns have documentation field with description and usage
- [ ] All 5 patterns have framework_compatibility field
- [ ] All 5 patterns have code_examples for all 4 frameworks (react, vue, svelte, vanilla)
- [ ] Pattern validation passes for all 5 patterns
</verification>

<success_criteria>
Layout and navigation patterns (5 total) updated with:
- Full documentation (description, usage, parameters, best_practices)
- Framework compatibility metadata
- Code examples for Vue and Svelte (React and vanilla already exist)
All patterns validate against the updated schema.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-framework-docs/04-03-SUMMARY.md`
</output>

---
phase: 04-multi-framework-docs
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .knowledge/patterns/forms/floating-label-input.yaml
  - .knowledge/patterns/forms/form-validation-states.yaml
  - .knowledge/patterns/feedback/toast-notification.yaml
  - .knowledge/patterns/feedback/loading-skeleton.yaml
autonomous: true

must_haves:
  truths:
    - "All forms patterns have documentation with parameter descriptions"
    - "All feedback patterns have documentation with usage instructions"
    - "All 4 patterns have Vue and Svelte code examples"
    - "All 4 patterns have framework_compatibility metadata"
  artifacts:
    - path: ".knowledge/patterns/forms/floating-label-input.yaml"
      provides: "Documented floating label input with all framework examples"
    - path: ".knowledge/patterns/feedback/toast-notification.yaml"
      provides: "Documented toast notification with all framework examples"
  key_links:
    - from: ".knowledge/patterns/forms/floating-label-input.yaml"
      to: ".knowledge/schema/pattern.schema.ts"
      via: "validation"
      pattern: "PatternSchema"
---

<objective>
Add documentation and multi-framework code examples to forms and feedback patterns.

Purpose: Update the 4 forms and feedback patterns with comprehensive documentation, framework compatibility metadata, and code examples for Vue and Svelte.
Output: Fully documented patterns with code_examples for all 4 frameworks.
</objective>

<execution_context>
@/Users/khairul/.claude/get-shit-done/workflows/execute-plan.md
@/Users/khairul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-multi-framework-docs/04-RESEARCH.md
@.knowledge/patterns/forms/floating-label-input.yaml
@.knowledge/patterns/feedback/toast-notification.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update forms patterns with documentation</name>
  <files>.knowledge/patterns/forms/floating-label-input.yaml, .knowledge/patterns/forms/form-validation-states.yaml</files>
  <action>
For each forms pattern (floating-label-input, form-validation-states):

1. Add documentation field:
```yaml
documentation:
  description: "[What this form pattern does and its UX benefits]"
  usage: "[How to implement in a form context]"
  parameters:
    - name: id
      type: string
      description: Unique identifier for input and label association
      required: true
    - name: label
      type: string
      description: Label text to display
      required: true
    - name: value
      type: string
      description: Current input value
      required: true
    - name: onChange
      type: "(value: string) => void"
      description: Callback when value changes
      required: true
  best_practices:
    - "Always associate label with input using htmlFor/for attribute"
    - "Ensure sufficient color contrast for accessibility"
```

2. Add framework_compatibility:
```yaml
framework_compatibility:
  react: "18.0+"
  vue: "3.0+"
  svelte: "4.0+"
  vanilla: true
```

3. Add Vue code example using v-model and Composition API:
```yaml
code_examples:
  vue: |
    <script setup lang="ts">
    interface Props {
      id: string;
      label: string;
      type?: string;
    }
    const props = withDefaults(defineProps<Props>(), { type: 'text' });
    const modelValue = defineModel<string>({ required: true });
    const hasValue = computed(() => modelValue.value.length > 0);
    </script>
    <template>
      <div class="floating-input-wrapper">
        <input :id="id" :type="type" v-model="modelValue" class="floating-input" placeholder=" " />
        <label :for="id" :class="['floating-label', { 'floating-label--active': hasValue }]">
          {{ label }}
        </label>
      </div>
    </template>
```

4. Add Svelte code example using bind:value.
  </action>
  <verify>
Run pattern validation: `npx tsx .knowledge/schema/validate-patterns.ts`
Both forms patterns should validate.
  </verify>
  <done>Both forms patterns have documentation, framework_compatibility, and Vue/Svelte code examples</done>
</task>

<task type="auto">
  <name>Task 2: Update feedback patterns with documentation</name>
  <files>.knowledge/patterns/feedback/toast-notification.yaml, .knowledge/patterns/feedback/loading-skeleton.yaml</files>
  <action>
For each feedback pattern (toast-notification, loading-skeleton):

1. Add documentation field:
```yaml
documentation:
  description: "[What this feedback pattern does and when to use it]"
  usage: "[How to trigger/display this feedback element]"
  parameters:
    - name: message
      type: string
      description: Message to display in toast
      required: true
    - name: type
      type: "'success' | 'error' | 'warning' | 'info'"
      description: Visual style variant
      required: false
      default: "'info'"
    - name: duration
      type: number
      description: Auto-dismiss time in milliseconds
      required: false
      default: "3000"
  best_practices:
    - "Use appropriate colors for each type (green=success, red=error)"
    - "Ensure toasts don't block important content"
    - "Provide dismiss button for accessibility"
```

2. Add framework_compatibility:
```yaml
framework_compatibility:
  react: "18.0+"
  vue: "3.0+"
  svelte: "4.0+"
  vanilla: true
```

3. Add Vue code example for toast notification:
```yaml
code_examples:
  vue: |
    <script setup lang="ts">
    type ToastType = 'success' | 'error' | 'warning' | 'info';
    interface Props {
      message: string;
      type?: ToastType;
      duration?: number;
    }
    const props = withDefaults(defineProps<Props>(), {
      type: 'info',
      duration: 3000,
    });
    const emit = defineEmits<{ dismiss: [] }>();
    const typeClasses = { success: 'bg-green-500', error: 'bg-red-500', warning: 'bg-yellow-500', info: 'bg-blue-500' };
    onMounted(() => {
      setTimeout(() => emit('dismiss'), props.duration);
    });
    </script>
    <template>
      <div :class="['toast', typeClasses[type]]" role="alert">
        {{ message }}
        <button @click="emit('dismiss')" aria-label="Dismiss">x</button>
      </div>
    </template>
```

4. Add Svelte code examples for both patterns.
  </action>
  <verify>
Run pattern validation: `npx tsx .knowledge/schema/validate-patterns.ts`
Both feedback patterns should validate.
  </verify>
  <done>Both feedback patterns have documentation, framework_compatibility, and Vue/Svelte code examples</done>
</task>

</tasks>

<verification>
- [ ] All 4 patterns have documentation field with description and parameters
- [ ] All 4 patterns have framework_compatibility field
- [ ] All 4 patterns have code_examples for all 4 frameworks
- [ ] Pattern validation passes for all 4 patterns
</verification>

<success_criteria>
Forms and feedback patterns (4 total) updated with:
- Full documentation (description, usage, parameters, best_practices)
- Framework compatibility metadata
- Code examples for Vue and Svelte
All patterns validate against the updated schema.
</success_criteria>

<output>
After completion, create `.planning/phases/04-multi-framework-docs/04-04-SUMMARY.md`
</output>
